# Core ETL pipeline dependencies (extract/transform/load + the search app)
requests==2.31.0
beautifulsoup4==4.12.3
pandas==2.2.2
nltk==3.9.1
sentence-transformers==3.1.1
pymongo==4.8.0
numpy==1.26.4
faiss-cpu==1.8.0

# App (FastAPI search UI) -- also listed separately in requirements-app.txt
# so the Docker image only installs what it needs to serve the app.
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.9

# NOTE: apache-airflow is intentionally NOT pinned here. Airflow ships
# its own constraints file and heavy dependency set that will fight
# with the pins above if installed into the same environment. Install
# it separately (see README) or run the DAG via the official Airflow
# Docker image, mounting dags/ into it.
